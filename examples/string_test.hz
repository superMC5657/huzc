fn main() -> i32 {
    # 字符串长度
    let s = "hello"
    print("len:", len(s))

    # 字符串拼接
    let a = "Hello, "
    let b = "World!"
    let c = concat(a, b)
    print(c)

    # 数值转字符串
    let num = 42
    let str = to_string(num)
    print("num as string:", str)

    return 0
}